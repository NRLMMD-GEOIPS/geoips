# # # This source code is subject to the license referenced at
# # # https://github.com/NRLMMD-GEOIPS.

# script migration = done
# product testing = ongoing

# Default values - if you do not have this exact test case available, call with available data files / sectors.
# This exact test case required for valid comparisons - remove "compare_path" argument if running a different
# set of arguments.

apiVersion: geoips/v1
interface: products
family: order_based
name: read_test
docstring: Read test.
package: geoips
test:
  fnames: !ENV ${GEOIPS_TESTDATA_DIR}/test_data_sgli/data/20251018T0233/*DL*.h5
spec:
  globals:
    product_name: IR-RGB
    reader_defined_area_def: False
    sector_list: ["korea"]
    minimum_coverage: 0 
  steps:
    reader:
      kind: reader
      name: sgli_l1b_hdf5
    arguments: {}
    product:
      kind: product
      name: IR-RGB
      arguments: {}
    output_formatter:
      kind: output_formatter
      name: imagery_clean
      arguments: {}
    filename_formatter:
      kind: filename_formatter
      name: geoips_fname
      arguments: {}
    output_checker:
      kind: output_checker
      name: PLUGIN_PROVIDED
      arguments: {
        compare_path: !ENV $GEOIPS_PACKAGES_DIR/geoips/tests/outputs/sgli.static.<product>.imagery_clean
      }
