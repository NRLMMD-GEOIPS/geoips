# # # This source code is subject to the license referenced at
# # # https://github.com/NRLMMD-GEOIPS.

# script migration = done 
# product testing = ongoing

# Default values - if you do not have this exact test case available, call with available data files / sectors.
# This exact test case required for valid comparisons - remove "compare_path" argument if running a different
# set of arguments.

apiVersion: geoips/v1
interface: products
family: order_based
name: absdiff-IR-BD
docstring: |
  The absolute difference between IR-BD between two Geokomsat AMI scan times.
package: geoips
test:
  fnames: !ENV ${GEOIPS_TESTDATA_DIR}/test_data_multi_scan_times/data/geokompsat-ami/LA/*ir112*.nc
spec:
  global-arguments:
    product_name: absdiff-IR-BD
    sector_list: ["geokompsat"]
    minimum_coverage: 0 
  steps:
    reader:
      kind: reader
      name: ami_netcdf
    arguments: {}
    product:
      kind: product
      name: absdiff-IR-BD
      arguments: {}
    output_formatter:
      kind: output_formatter
      name: imagery_clean
      arguments: {}
    filename_formatter:
      kind: filename_formatter
      name: geoips_fname
      arguments: {}
    output_checker:
      kind: output_checker
      name: PLUGIN_PROVIDED
      arguments: {
        compare_path: !ENV $GEOIPS_PACKAGES_DIR/geoips/tests/outputs/ami.static.mst.<product>.imagery_clean
      }
