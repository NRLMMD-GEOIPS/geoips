# # # This source code is subject to the license referenced at
# # # https://github.com/NRLMMD-GEOIPS.

# script migration = done 
# product testing = ongoing

# Default values - if you do not have this exact test case available, call with available data files / sectors.
# This exact test case required for valid comparisons - remove "compare_path" argument if running a different
# set of arguments.

apiVersion: geoips/v1
interface: products
family: order_based
name: ami-Infrared
docstring: The Infrared product_defaults configuration for the Geokompsat AMI product.
package: geoips
test:
  fnames: !ENV ${GEOIPS_TESTDATA_DIR}/test_data_ami/data/20231208_0300_daytime/gk2a_ami_le1b_*.nc
spec:
  global-arguments:
    product_name: Infrared
    sector_list: ["geokompsat"]
  steps:
    reader:
      kind: reader
      name: ami_netcdf
      arguments: {
        resampled_read: True,
      }
    product:
      kind: product
      name: Infrared
      arguments: {}
    output_formatter:
      kind: output_formatter
      name: imagery_clean
      arguments: {}
    filename_formatter:
      kind: filename_formatter
      name: geoips_fname
      arguments: {}
    output_checker:
      kind: output_checker
      name: PLUGIN_PROVIDED
      arguments: {
        compare_path: !ENV $GEOIPS_PACKAGES_DIR/geoips/tests/outputs/ami.static.<product>.imagery_clean
      }
