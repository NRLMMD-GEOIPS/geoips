$id: product_defaults.bases.unvalidated
type: object
description: |
  The default product_defaults unvalidated configuration. This defines a YAML file 
  that has yet to be validated by GeoIPS software.
required:
  - spec
properties:
  spec:
    type: object
    unevaluatedProperties: true # Family/schema specifies order of ops but not format
