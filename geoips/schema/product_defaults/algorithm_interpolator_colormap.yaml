$id: product_defaults.algorithm_interpolator_colormap
$ref: bases.top
type: object
description: |
  The default product_defaults algorithm_interpolator_colormap configuration. Requires spec, which specifies
  the algorithm_interpolator_colormap structure. This includes the algorithm type, colormap, interpolator, display_name, and more.
required:
  - spec
properties:
  family:
    const: algorithm_interpolator_colormap
  spec:
    unevaluatedProperties: false
    $ref: "product_defaults.specs.algorithm_interpolator_colormap"
