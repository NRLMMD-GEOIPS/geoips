$id: "plugin_defaults.algorithm_colormap"
$ref: "base"
type: object
required:
  - spec
properties:
  spec:
    type: object
    additionalProperties: false
    required:
      - algorithm
      - colormap
    properties:
      algorithm:
        $ref: "plugin_defaults.algorithm"
      colormap:
        $ref: "plugin_defaults.colormap"
      coverage_checker:
        $ref: "plugin_defaults.coverage_checker"
      mtif_type:
        type: string
