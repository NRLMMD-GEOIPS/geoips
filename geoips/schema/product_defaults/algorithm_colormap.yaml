$id: product_defaults.algorithm_colormap
$ref: "base"
type: object
required:
  - spec
properties:
  spec:
    type: object
    additionalProperties: false
    required:
      - algorithm
      - colormap
    properties:
      algorithm:
        $ref: "product_defaults.algorithm"
      colormap:
        $ref: "product_defaults.colormap"
      coverage_checker:
        $ref: "product_defaults.coverage_checker"
      mtif_type:
        type: string
