$id: product_defaults.algorithm_colormap
$ref: bases.top
type: object
description: |
  The default product_defaults algorithm_colormap configuration. Requires spec, which specifies
  the algorithm_colormap structure. This includes the algorithm type, colormap, display_name, and more.
required:
  - spec
properties:
  family:
    const: algorithm_colormap
  spec:
    unevaluatedProperties: false
    $ref: "product_defaults.specs.algorithm_colormap"
