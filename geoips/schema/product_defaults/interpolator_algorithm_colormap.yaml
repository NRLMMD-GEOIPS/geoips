$id: product_defaults.interpolator_algorithm_colormap
$ref: bases.top
type: object
description: |
  The default product_defaults interpolator_algorithm_colormap configuration. Requires spec, which specifies
  the interpolator_algorithm_colormap structure. This includes the algorithm type, colormap, interpolator, display_name, and more.
required:
  - spec
properties:
  family:
    const: interpolator_algorithm_colormap
  spec:
    unevaluatedProperties: false
    $ref: "product_defaults.specs.interpolator_algorithm_colormap"
