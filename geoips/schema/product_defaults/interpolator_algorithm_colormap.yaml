$id: product_defaults.interpolator_algorithm_colormap
$ref: bases.base
type: object
required:
  - spec
properties:
  spec:
    type: object
    # ONLY allow algorithm, interpolator, colormap, coverage_checker,
    # windbarbs, and mtif_type fields under "spec". If additional fields are required
    # in the future, must update schema appropriately.
    # REQUIRE algorithm, interpolator, and colormap
    additionalProperties: false
    required:
      - algorithm
      - interpolator
      - colormap
    properties:
      algorithm:
        $ref: "bases.algorithm"
      interpolator:
        $ref: "bases.interpolator"
      colormap:
        $ref: "bases.colormap"
      coverage_checker:
        $ref: "bases.coverage_checker"
      windbarbs:
        $ref: "bases.windbarbs"
      mtif_type:
        type: string
