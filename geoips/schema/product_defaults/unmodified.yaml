$id: product_defaults.unmodified
$ref: product_defaults.bases.unvalidated # Family specifies order of ops but not formatting
description: |
  The default product_defaults unmodified configuration.
properties:
  family:
    const: unmodified
