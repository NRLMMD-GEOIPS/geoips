$id: product_defaults.interpolator_algorithm
$ref: base
type: object
required:
  - spec
properties:
  spec:
    type: object
    additionalProperties: false
    required:
      - algorithm
      - interpolation
    properties:
      algorithm:
        $ref: "product_defaults.algorithm"
      interpolator:
        $ref: "product_defaults.interpolator"
      coverage_checker:
        $ref: "product_defaults.coverage_checker"
      windbarbs:
        $ref: "product_defaults.windbarbs"
      mtif_type:
        type: string
