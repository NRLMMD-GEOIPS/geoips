$id: product_defaults.interpolator_algorithm
$ref: bases.base
type: object
required:
  - spec
properties:
  spec:
    type: object
    additionalProperties: false
    required:
      - algorithm
      - interpolation
    properties:
      algorithm:
        $ref: "bases.algorithm"
      interpolator:
        $ref: "bases.interpolator"
      coverage_checker:
        $ref: "bases.coverage_checker"
      windbarbs:
        $ref: "bases.windbarbs"
      mtif_type:
        type: string
