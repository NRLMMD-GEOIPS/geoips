$id: product_defaults.interpolator_algorithm
$ref: bases.top
type: object
description: |
  The default product_defaults interpolator_algorithm configuration. Requires spec,
  which specifies the interpolator_algorithm structure. This includes the algorithm
  type, interpolator, display_name, and more. See
  schema/product_defaults/specs/interpolator_algorithm.yaml for more information.
required:
  - spec
properties:
  family:
    const: interpolator_algorithm
  spec:
    unevaluatedProperties: false
    $ref: "product_defaults.specs.interpolator_algorithm"
