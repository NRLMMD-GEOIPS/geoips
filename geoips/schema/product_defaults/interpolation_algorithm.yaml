$id: plugin_defaults.interpolator_algorithm
$ref: base
type: object
required:
  - spec
properties:
  spec:
    type: object
    additionalProperties: false
    required:
      - algorithm
      - interpolation
    properties:
      algorithm:
        $ref: "plugin_defaults.algorithm"
      interpolator:
        $ref: "plugin_defaults.interpolator"
      coverage_checker:
        $ref: "plugin_defaults.coverage_checker"
      mtif_type:
        type: string
