$id: products.single
$ref: bases.top
description: |
  The default products single product configuration. Requires spec, which defines the
  structure of single. This is the structure for a single product.
required:
  - spec
properties:
  spec:
    type: object
    required:
      - product
    properties:
      product:
        $ref: products.bases.product
