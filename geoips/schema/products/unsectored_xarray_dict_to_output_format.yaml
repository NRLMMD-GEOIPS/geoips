$id: products.unsectored_xarray_dict_to_output_format
$ref: bases.top
type: object
description: |
  The default products unsectored_xarray_dict_to_output_format configuration. Requires spec, which defines the structure
  of the unsectored_xarray_dict_to_output_format. Has additional properties family and source_name, which define the 
  family in which the unsectored_xarray_dict_to_output_format resides, and the source_name of where the 
  unsectored_xarray_dict_to_output_format is derived from.
required:
  - spec
properties:
  family:
    const: unsectored_xarray_dict_to_output_format
  source_names:
    type: array
    items:
      type: string
