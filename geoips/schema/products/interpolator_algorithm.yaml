$id: products.interpolator_algorithm
$ref: bases.top
type: object
required:
  - spec
properties:
  family:
    const: interpolator_algorithm
  source_names:
    type: array
    items:
      type: string
  spec:
    unevaluatedProperties: false
    $ref: "product_defaults.specs.interpolator_algorithm"
    properties:
      variables:
        type: array
        items:
          type: string
