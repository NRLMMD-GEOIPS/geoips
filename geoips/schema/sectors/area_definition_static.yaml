$id: sectors.area_definition_static
$ref: bases.top
required:
  - spec
  - metadata
properties:
  metadata:
    $ref: "sectors.metadata_families.static"
  spec:
    $ref: "sectors.specs.static_sector_spec"
