$id: sectors.area_definition_atmosriver
$ref: bases.top
required:
  - spec
  - metadata
properties:
  metadata:
    $ref: "sectors.metadata_families.atmosriver"
  spec:
    $ref: "sectors.specs.static_sector_spec"
