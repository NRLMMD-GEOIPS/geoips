$id: sectors.area_definition_volcano
$ref: bases.top
required:
  - spec
  - metadata
properties:
  metadata:
    $ref: "sectors.metadata_families.volcano"
  spec:
    $ref: "sectors.specs.static_sector_spec"
