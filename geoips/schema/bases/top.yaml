$id: bases.top
type: object
description: "The required elements for a GeoIPS schema YAML file."
required:
  - interface
  - family
  - name
  - docstring
properties:
  interface:
    $ref: bases.interface
  family:
    $ref: bases.family
  name:
    $ref: bases.name
  docstring:
    $ref: bases.docstring
  package:
    type: string
  relpath:
    type: string
  abspath:
    type: string
