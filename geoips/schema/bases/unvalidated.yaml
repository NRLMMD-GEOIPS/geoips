$id: bases.unvalidated
$ref: bases.top
type: object
unevaluatedProperties: false
required:
  - spec
properties:
  spec:
    type: object
    unevaluatedProperties: true # Family/schema specifies order of ops but not format
