# # # This source code is subject to the license referenced at
# # # https://github.com/NRLMMD-GEOIPS.

# script migration = done
# product testing = ongoing

# Default values - if you do not have this exact test case available, call with available data files / sectors.
# This exact test case required for valid comparisons - remove "compare_path" argument if running a different
# set of arguments.

apiVersion: geoips/v1
interface: workflows
family: order_based
name: smap_unsectored_text_winds
docstring: |
  Process SMAP REMSS wind NetCDF data to generate unsectored text winds output.
package: geoips
test:
  fnames: !ENV ${GEOIPS_TESTDATA_DIR}/test_data_smap/data/RSS_smap_wind_daily_2021_09_26_NRT_v01.0.nc
  outputs:
    output_formatter:
      full_test_policy: on_token_mismatch # always | never
      compare_path: !ENV ${GEOIPS_PACKAGES_DIR}/geoips/tests/outputs/smap.unsectored.text_winds
      output_checker_name: text
spec:
  globals:
    product_name: unsectored
    reader_defined_area_def: False
  steps:
    reader:
      kind: reader
      name: smap_remss_winds_netcdf
    # smap_spd_unsectored:
    #   kind: product
    #   name: [smap-spd, unsectored]
    #   depends_on: [reader]
    filename_formatter:
      kind: filename_formatter
      name: text_winds_full_fname
    output_formatter:
      kind: output_formatter
      name: text_winds
