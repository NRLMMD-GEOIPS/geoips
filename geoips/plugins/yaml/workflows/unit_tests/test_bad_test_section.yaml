apiVersion: geoips/v1
interface: workflows
family: order_based
name: test_bad_test_section
docstring: |
  This is a dummy plugin and is used for unit testing. Not intended to be used in
  operations.
description: 11.2 µm Infrared.
test:
  fnames: !ENV ${GEOIPS_TESTDATA_DIR}/test_data_abi/data/goes16_20200918_1950/*
  not_a_real_argument:
    compare_path: !ENV ${GEOIPS_PACKAGES_DIR}/geoips/tests/outputs/abi.static.<product>.imagery_clean
    logging_level: info
spec:
  steps:
    reader:
      kind: reader
      name: abi_netcdf
      arguments:
        variables: ['B14BT']
    product:
      kind: product
      name: [abi, Infrared]
      arguments: {}
