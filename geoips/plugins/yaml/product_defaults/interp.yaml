# This YAML product config is useful only as a "product_template" to
# define products directly within product_inputs/<source>.yaml
interface: product_defaults
family: interpolator
name: interp
docstring: |
  The interp product_defaults configuration.
spec:
  interpolator:
    plugin:
      name: pyresample_wrappers.interp_nearest
      arguments: {}
