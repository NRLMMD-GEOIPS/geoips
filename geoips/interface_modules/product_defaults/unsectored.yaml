# This YAML product config is useful only as a "product_template" to
# define products directly within product_lists/<source>.yaml
interface: product_defaults
metadata:
  name: unsectored
  family: unsectored_xarray_dict_to_output_format
spec: {}
