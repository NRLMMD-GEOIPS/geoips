interface: product_defaults
family: interpolator_algorithm_colormap
metadata:
  name: RainNearest
  short-description: "RainNearest"
  long-description: ""
spec:
  algorithm:
    plugin_name: single_channel
    arguments:
      output_data_range: [0.05, 50.0]
      min_outbounds: mask
      max_outbounds: crop
      norm: null
      inverse: null
  mtif_type: rain
  colormap:
    plugin_name: pmw_tb.cmap_Rain
    arguments:
      data_range: [0.05, 50.0]
  interpolation:
    plugin_name: pyresample_wrappers.interp_nearest
    arguments: {}
